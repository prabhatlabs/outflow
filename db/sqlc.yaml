version: "2"

sql:
  - engine: postgresql
    schema: "../db/migrations"
    queries: "../db/queries"

    gen:
      go:
        package: db
        out: "../server/internal/db"
        sql_package: pgx/v5
